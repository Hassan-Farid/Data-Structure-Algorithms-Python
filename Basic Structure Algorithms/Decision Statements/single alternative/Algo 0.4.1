
Algo 0.4.1: Using Single Alternative

Single_Alter(int VAR):

	START
Step 01: [Reading input]
	Read VAR
Step 02: [Checking if even]
	IF VAR % 2 == 0 THEN:
		WRITE (VAR, " is an Even number")
	END IF
	EXIT
	
