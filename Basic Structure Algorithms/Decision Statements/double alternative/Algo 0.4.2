
Algo 0.4.2: Using Double Alternative

Double_Alter(int VAR):

	START
Step 01: [Reading input]
	Read VAR
Step 02: [Applying Double Alterntive]
	[a. Checking if even]
	IF VAR % 2 == 0 THEN:
		WRITE(VAR, "is an Even Number")
	[b. Declaring as Odd]
	ELSE THEN:
		WRITE(VAR, "is an Odd Number")
	END IF
	EXIT
	
	