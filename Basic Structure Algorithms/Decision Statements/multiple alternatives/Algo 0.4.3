
Algo 0.4.3: Using Multiple ALternatives

Multiple_Alter(int VAR):

	START
Step 01: [Reading input]
	Read VAR
Step 02: [Using Multiple ALternatives]
	[a. Checking if greater than 0]
	IF VAR > 0 THEN:
		WRITE(VAR, "is greater than 0")
	[b. Checking if less than 0]
	ELSE IF VAR < 0 THEN:
		WRITE(VAR, "is less than 0")
	[c. Declaring as 0]
	ELSE THEN:
		WRITE(VAR, "is equal to 0")
	END IF
	EXIT
	
	
	